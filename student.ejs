<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f2f2f2;
        }
        h1 {
            text-align: center;
            margin-top: 20px;
        }
        table {
            border-collapse: collapse;
            width: 100%;
            margin-top: 20px;
            background-color: #fff;
        }
        th, td {
            border: 1px solid #dddddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        tr:hover {
            background-color: #ddd;
        }
        td[colspan="5"] {
            text-align: center;
        }
        button {
            padding: 10px 20px;
            background-color: #007bff;
            border: none;
            color: #fff;
            border-radius: 5px;
            cursor: pointer;
            margin-top:2%;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Code Submission List</h1><br>

    <button onclick="window.location.href='/'">Submit Another Response</button>

    <table>
        <tr>
            <th>S.no</th>
            <th>Username</th>
            <th>Code Language</th>
            <th>Input</th>
            <th>Output</th>
        </tr>
        <% if (records) { %>
            <% for(var i=0; i<records.length; i++) { %>
                <tr>
                    <td><%= records[i].id %></td>
                    <td><%= records[i].username %></td>
                    <td><%= records[i].pcl %></td>
                    <td><%= records[i].stdin %></td>
                    <td><%= records[i].code %></td>
                </tr>
            <% } %>
        <% } else { %>
            <tr>
                <td colspan="5">No records found.</td>
            </tr>
        <% } %>
    </table>
</body>
</html>
